enter:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: public
      threads: 1
      type: postgres
      user: airflow
  target: dev
data_pipeline:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: public
      threads: 1
      type: postgres
      user: airflow
  target: dev